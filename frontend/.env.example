# API Configuration
# Set this to your backend server IP for external access
# VITE_API_URL=http://192.168.1.17:5000/api

# Note: Frontend uses HTTPS (for crypto.subtle API support)
# Backend uses HTTP (Flask development server)
# This is normal for development - browser will show a warning for the self-signed certificate
